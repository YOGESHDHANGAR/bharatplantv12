const validateRequired = (value) => !!value.length;

export function validateRecycleBin(recycleBin) {
  return {
    // recycleBinName: !validateRequired(recycleBin.recycleBinName)
    //   ? "Ledger Name is Required"
    //   : "",
  };
}
