const validateRequired = (value) => !!value.length;

export function validateSalary(salary) {
  return {
    // salaryName: !validateRequired(salary.salaryName) ? "Salary Name is Required" : "",
  };
}
